{
  "name": "cos-marketplace",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "PORT=3000 node --watch server.mjs",
    "start": "node server.mjs",
    "test": "node --test tests/server.test.mjs"
  }
}
